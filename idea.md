# 基于 Rust 构建的高性能时间序列数据库

项目计划书：应该包括功能架构，技术架构，关键技术，实施计划。

实施计划（测试驱动开发，各个阶段测试要采用不同业务的真实数据)：功能开发，单元测试；联调，集成测试；性能测试；系统部署；安全测试；上线联调，上线；

## 功能架构

构建一个高性能的时序数据库；

### 生成时序图

### 多业务隔离

```
股票行情、IOT、金融行情、订单、支付、库存、短信下发
复制到不同的 DB 实例，互不干扰。
```

### 维度表设计

对不同业务指标 tag 进行编码。

### 管理页面

业务数据数据 :聚合函数,支持 SQL 支持 ,页面查看数据

业务仪表盘

性能仪表盘

### 数据分析 指标 维度

按时间维度 小时 天 周 月 生成轻度汇总数据库，轻度汇总数据库采用分隔符压缩；

按其他维度生成轻度汇总数据库，也采用前缀分隔符进行压缩；

轻度汇总数据在明细数据写入时候异步计算；

轻度汇总数据单独保存到 rocksdb，一般按时间线性增加不会修改，避免重复计算；

按时间维度查看趋势；

每种业务一个轻度汇总 db，每个汇总维度一个 cf；轻度汇总数据长期保存；

key分业务分隔符前缀压缩策略；

## 技术架构

### 使用Rust开发

### 界面开发

ExpoGo

### 可配置

config.ini 提供各种配置参数；

### 端口对外服务

NNG提供服务接口；命令行启动提供服务；

### 索引

时间索引：跳表（范围查询）

标签索引：倒排索引（Tag 查询）

### **压缩** ：

时间戳：Delta 编码

数值： **Gorilla XOR** （浮点）

字符串：字典编码

### 查询引擎：

**向量化执行引擎，向量化 + SIMD 优化**

聚合 / 分组极快（列式 + 向量化）

复杂 SQL/JOIN

### 存储引擎：采用RocksDB数据持久化

充分利用RocksDB的插件机制，定制引擎时序数据库高性能组件，c++开发插件。

**数据模型** ：Measurement + Tags（索引）+ Fields（数值）+ Timestamp

#### RowKey + Qualifier 机制

**`30秒定长块 + 微秒列偏移`**

- **时序数据优化**：专为时间序列数据设计的存储结构
- **30秒定长块存储** - 优化压缩率和查询性能
- **微秒级时间戳精度** - 满足高频交易需求
  **核心特性：** 30秒定长块 + 微秒列偏移，优化达到生产级别使用。

#### 冷热数据分离

##### 借鉴 Kvrocks 的元数据与数据 CF 处理

借鉴 Kvrocks 处理元数据与数据 RocksDB 的 ColumnFamily（CF），按自然日分 ColumnFamily，实现冷热数据分离。

##### *按自然日分 ColumnFamily（CF）

```
每天自动新建一个 CF
冷数据 CF 可单独设置 ZSTD 压缩 + 关闭自动 Compaction
想删 30 天前数据，直接 Drop 整个 CF，秒级完成，不产生 Compaction 抖动。
```

### 插件机制，可配置

业务插件，可配置增加业务；

查询插件

存储插件

## 测试驱动开发

#### 功能开发与重构提供完备测试覆盖；

#### 系统集成开发与测试

#### 使用真实数据进行测试驱动；

git clone https://github.com/timescale/tsbs
生成 DevOps 4天数据（4000设备）
./tsbs_generate_data --use-case="devops" --scale=4000 --timestamp-end="2025-01-05T00:00:00Z"

## 系统部署与发布

### GITHUB发布

通过github提供windows macos linux多操作系统发布平台；适配主流CPU；触发CI构建；
